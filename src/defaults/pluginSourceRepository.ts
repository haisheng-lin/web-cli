import { PluginSourceRepository } from '@definitions';

const defaultPluginSourceRepository: PluginSourceRepository = {
  pluginSource: 'github',
  pluginSourceUrl: 'github.com',
};

export default defaultPluginSourceRepository;
